# Artifacts
This directory contains research outputs selected for publication.

- `public/` contains one inspectable bundle per registered experiment.
- Local or temporary lab output does not belong here.
- Frozen corpus releases belong under `research/frozen/`, not under artifacts.

Public bundles are generated and validated by `packages/lab-exporter`.
