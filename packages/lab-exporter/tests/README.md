# Lab Exporter Tests

V1 uses `npm.cmd run research:validate` as the executable test surface.

Future tests should cover:

- schema validation failures;
- checksum drift;
- missing public artifact files;
- duplicate registry IDs;
- portal data build output.
